// Online C++ compiler to run C++ program online
#include <iostream>
#include<fstream>
using namespace std;
int main(){
    ifstream file("ab.txt");
    string line;
    while (getline(file,line)){
        cout<<line<<endl;
    }
    file.close();
    return 0;
}
